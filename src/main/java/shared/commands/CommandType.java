package shared.commands;

/**
 * Типы поддерживаемых команд.
 */
public enum CommandType {
    HELP,
    INFO,
    SHOW,
    ADD,
    UPDATE,
    REMOVE_BY_ID,
    CLEAR,
    EXECUTE_SCRIPT,
    EXIT,
    REMOVE_HEAD,
    ADD_IF_MIN,
    REMOVE_GREATER,
    MIN_BY_COORDINATES,
    MAX_BY_ID,
    COUNT_GREATER_THAN_GENRE
}